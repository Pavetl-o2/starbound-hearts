{
  "name": "starbound-hearts",
  "version": "0.1.0",
  "description": "A procedural dating sim set aboard a deep-space vessel",
  "scripts": {
    "build": "node build.js",
    "dev": "python3 -m http.server 8080 || python -m http.server 8080"
  },
  "engines": {
    "node": ">=18"
  }
}
